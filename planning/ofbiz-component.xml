<?xml version="1.0" encoding="UTF-8"?>

<ofbiz-component name="planning"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="https://ofbiz.apache.org/dtds/ofbiz-component.xsd">
    <!-- define resource loaders; most common is to use the component resource loader -->
    <resource-loader name="main" type="component"/>

    <!-- place the config directory on the classpath to access configuration files -->
    <classpath type="dir" location="config"/>
    <classpath type="dir" location="dtd"/>
    <classpath type="dir" location="entitydef"/>
    <classpath type="dir" location="script"/>
    <classpath type="dir" location="src"/>
    <classpath type="dir" location="webapp"/>
    <classpath type="dir" location="widget"/>
    <classpath type="jar" location="lib/*"/>
    <classpath type="dir" location="src/main/java"/>
    <classpath type="dir" location="src/main/resources"/>
    
    <!-- entity resources: model(s), eca(s), group, and data definitions -->
    <entity-resource type="model" reader-name="main"
    	loader="main" location="entitydef/entitymodel.xml"/>
    <entity-resource type="data" reader-name="main"
    	loader="main" location="data/PlanningSeedData.xml"/>
    
    <!-- 
    <entity-resource type="data" reader-name="main"
        loader="main" location="data/TechDataCalendarSeedData.xml"/>
    -->

    <!-- Puede que estas líneas sobren -->
    <entity-resource type="data" reader-name="seed" loader="main" location="data/PlanningTypeData.xml"/>
    <entity-resource type="data" reader-name="seed" loader="main" location="data/PlanningSecurityPermissionSeedData.xml"/>
    <entity-resource type="data" reader-name="demo" loader="main" location="data/PlanningSecurityGroupDemoData.xml"/>
    <entity-resource type="data" reader-name="demo" loader="main" location="data/PlanningDemoData.xml"/>

    <test-suite loader="main" location="testdef/PlanningTests.xml"/>
    <!-- Hasta aquí -->

    <!-- Service Resources -->
    <service-resource type="model" loader="main" location="servicedef/services.xml"/>

    <!-- web applications; will be mounted when using the embedded container -->
    <webapp name="planning"
        title="Planning"
        server="default-server"
        location="webapp/planning"
        base-permission="OFBTOOLS,PLANNING"
        mount-point="/planning"/>
</ofbiz-component>
